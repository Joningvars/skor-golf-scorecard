import 'package:flutter/material.dart';

class CourseSelectScreen extends StatelessWidget {
  const CourseSelectScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Course select screen')),
    );
  }
}
